# Exemplo de arquivo de ambiente para o compilador C em PHP
# Cada flag controla uma parte específica da saída do compilador.

# Ativa (1) ou desativa (0) testes de debug internos (usado no PHPUnit, etc.)
DEBUG_TESTS=0

# Imprime todo o log (léxico, sintático, semântico, ASM) no console
CONSOLE_LOG=true

# Grava todo o log (léxico, sintático, semântico, ASM) em output/log/<nome>.log
WRITE_LOG=true

# Grava apenas o código ASM gerado em output/assembly/<nome>.asm
WRITE_ASM=false
